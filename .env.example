# Database Configuration
# DB_HOST should be your Windows IP when running from WSL
# You can find it with: ipconfig /all (look for WSL IP or use host.docker.internal)
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=pgtest
DB_NAME=food_order_tracking
